<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Results page</title>
</head>
<body>
    <style>
        h2 {
            text-align: center;
            font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
            padding: 1rem;
        }

        .results ul {
            max-width: 100%;
            list-style-type: none;

            display: flex;
            justify-content: space-around;
            flex-wrap: wrap;
        }

        #job-card {
            height: fit-content;
            width: 20rem;
            min-height: 15rem;
            padding: 3rem;
            border-radius: 10px;
            box-shadow: 0px 0px 7px gray;
            margin-bottom: 1rem;

            display: flex;
            flex-direction: column;
            overflow: hidden;

            background: #bdc3c7;  /* fallback for old browsers */
            background: -webkit-linear-gradient(to right, #2c3e50, #bdc3c7);  /* Chrome 10-25, Safari 5.1-6 */
            background: linear-gradient(to right, #a8c4e0, #bdc3c7); /* W3C, IE 10+/ Edge, Firefox 16+, Chrome 26+, Opera 12+, Safari 7+ */
        }

        .results h3 {
            text-align: start;
            text-transform: capitalize;

            font-size: 1.5rem;
            margin: 0.2rem 0 1rem 0;
        }

        #job-address {
            color: rgb(69, 68, 68);
        }

        #comp-name {
            font-size: 1.3rem;
            font-weight: bold;
        }

        #job-skill {
            font-size: 1.2rem;
            text-transform: capitalize;
        }

        #job-type {
            text-transform: capitalize;
        }

        #job-card * {
            margin: 0.5rem 0 1rem 0;
            text-wrap: wrap;
            font-family:Arial, Helvetica, sans-serif;
        }
    </style>

    <div class="results container">
        <h2>
            Following jobs are best match to your desired title and skills
        </h2>
        <ul>
            {% for jobLists in data %}
            <li id="job-card">
                {% for jobs in jobLists %}
                    {% if forloop.counter == 1 %}
                        <span id="job-address">Address: {{jobs}}</span>
                    {% elif forloop.counter == 2 %}
                        <h3 id="job-title">{{jobs}}</h3>
                    {% elif forloop.counter == 3 %}
                        <span id="comp-name">{{jobs}}</span>
                    {% elif forloop.counter == 4 %}
                        <span id="job-skill">Skills required: {{jobs}}</span>
                    {% elif forloop.counter == 5 %}
                        <span id="job-type">{{jobs}}</span>
                    {% endif %}
                {% endfor %}
            </li>
            {% endfor %}
        </ul>
    </div>
</body>
</html>